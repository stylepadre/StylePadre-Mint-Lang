component NavSeparator {
    style separator {
    border-left: 1px solid rgba(255, 255, 255, 0.2);
    margin: 0 15px;
    height: 30px;
    }

    fun render : Html {
        <div::separator/>
    }
}
